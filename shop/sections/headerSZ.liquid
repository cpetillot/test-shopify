{% assign menu = linklists[section.settings.navbar] %}


<section id="section-{{ section.id }}" class="section" data-section-id="{{ section.id }}" data-section-type="header">
  <div class="header">

  </div>
</section>

{% schema %}
  {
    "name": "header",
    "settings": [
      {
        "type": "image_picker",
        "id": "logo_header",
        "label": "Logo image"
      },
      {
        "type": "link_list",
        "id": "navbar",
        "label": "Menu"
      }
    ],
    "presets": [
      {
        "name": "header",
        "category": "Header"
      }
    ]
  }
{% endschema %}
